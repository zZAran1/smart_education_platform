# 智慧教育平台 系统设计文档

| 文档版本 | V1.2 | 编写日期 | 2026-09-05 |
| --- | --- | --- | --- |
| 项目名称 | 智慧教育平台 | 关联文档 | 《需求规格说明书 V1.1》 |
| 技术架构 | SpringBoot 3.4.4 + MyBatis-Plus 3.5.10 + Vue3 + MySQL 8 + Redis | 架构基线 | 复用 VolunteerSystem 项目架构约定 |

> 本文档的目录结构、分层契约、鉴权链路、异常体系、Redis 用法、前端组织方式均对齐既有 VolunteerSystem 项目（`volunteer_system_backend` / `volunteer_system_frontend`），保证团队开发习惯一致。

---

## 1. 总体设计

### 1.1 技术选型（与 VolunteerSystem 对齐）

| 层次 | 技术 | 版本 | 说明 |
| --- | --- | --- | --- |
| 后端框架 | Spring Boot | 3.4.4 | JDK 21 |
| ORM | mybatis-plus-spring-boot3-starter | 3.5.10 | CRUD、分页插件（本项目新增）、逻辑删除 |
| 数据库 | mysql-connector-j | 8.0.x | InnoDB，utf8mb4 |
| 缓存 | spring-boot-starter-data-redis + spring-boot-starter-cache | — | Spring Cache 注解缓存 + StringRedisTemplate 直用 |
| 认证 | jjwt-api / impl / jackson | 0.12.6 | 无状态登录认证 |
| 密码加密 | jbcrypt + spring-security-crypto | 0.4 | 仅引入 crypto，不启用 Security 自动配置 |
| 图形验证码 | hutool-all（CircleCaptcha） | 5.8.25 | 登录前图形验证码 |
| 对象转换 | MapStruct + lombok-mapstruct-binding | 1.6.3 / 0.2.0 | `Converter.INSTANCE` 统一转换 |
| 校验 / 其他 | spring-boot-starter-validation、Lombok | 1.18.30 | DTO 校验、代码简化 |
| 前端 | Vue 3.5 + Vite + vue-router 4 + axios + TypeScript | — | 无 Pinia、无 UI 组件库；reactive 单例状态管理 |

### 1.2 总体架构

```
┌────────────┐   HTTP/JSON    ┌──────────────────────────────────────────┐
│  Web 前端   │ ─────────────→ │               后端服务                     │
│ Vue3+axios │ ←───────────── │ Controller(DTO/VO+Result) → Service → Mapper│
│ reactive单例│                │        ↑                                 │
└────────────┘                │ AuthInterceptor + UserContext(ThreadLocal)│
┌────────────┐                │ Spring Cache(Redis) + GlobalExceptionHandler│
│  管理端     │                └──────────┬──────────────┬────────────────┘
└────────────┘                           │              │
                                   ┌─────▼─────┐  ┌─────▼─────┐
                                   │  MySQL 8  │  │   Redis   │
                                   └───────────┘  └───────────┘
```

### 1.3 后端工程结构（对齐 VolunteerSystem 包组织）

根包 `com.example.edu_platform`：

```
├── EduPlatformApplication.java  # @SpringBootApplication + @MapperScan + @EnableCaching + @EnableScheduling
├── config/        RedisConfig、WebMvcConfig、MybatisPlusConfig(分页插件, 本项目新增)
├── controller/    UserController、CourseController、JobController、OrderController、AdminController
├── converter/     Converter（MapStruct 接口, Converter.INSTANCE 统一调用）
├── exception/     BaseBusinessException + 各业务子类(固定错误码)
├── handler/       GlobalExceptionHandler（注意修正 VolunteerSystem 中 hander 的拼写）
├── interceptor/   AuthInterceptor
├── mapper/        UserMapper、CourseMapper、JobMapper…（均 extends BaseMapper，复杂 JOIN 走 XML）
├── model/
│   ├── dto/       LoginDTO、RegisterDTO、CourseQueryDTO、UpdateProfileDTO、ApplyJobDTO…
│   ├── entity/    Users、Course、CourseChapter、Job、Company、OrderInfo…
│   └── vo/        LoginVO、CaptchaVO、CourseCardVO、CourseDetailVO、JobDetailVO…
├── result/        Result<T>
├── service/       接口 + impl/（继承 ServiceImpl，链式 lambdaQuery/lambdaUpdate）
├── task/          定时任务（如每日刷新过期职位/课程状态）
└── util/          JwtUtil、UserContext、BCryptPasswordUtil
resources/
├── application.yml
└── mapper/        # 复杂多表 JOIN 查询 XML（resultType 直接映射 VO）
```

### 1.4 DTO / VO / Entity 分离规范

| 类型 | 用途 | 命名示例 | 流转位置 |
| --- | --- | --- | --- |
| Entity | 与表一一对应，`@Data + @TableName + @TableId`，**字段保留下划线命名** | `Users` / `Course` | 仅 Service ↔ Mapper |
| DTO | 接收前端参数（`jakarta.validation` 校验注解） | `LoginDTO` / `CourseQueryDTO` | Controller 入参 |
| VO | 返回前端视图对象，剔除敏感字段、组装关联数据 | `CourseDetailVO` / `JobDetailVO` | Controller 出参 |

- Controller 只收 DTO、返 VO 并包 `Result`，不写业务逻辑。
- 对象转换统一走 `Converter.INSTANCE`（MapStruct，`defaultComponentModel=spring`）。
- 复杂多表 JOIN 写在 Mapper XML，`resultType` 直接映射 VO，避免内存拼装。

**CourseQueryDTO 字段示例（下划线命名约定）：**

| 字段 | 类型 | 校验 | 说明 |
| --- | --- | --- | --- |
| type | Integer | @NotNull | 0 理论 1 实训 2 认证 |
| tech_system_id / tech_direction_id / level | Long / Long / Integer | 可空 | 筛选条件 |
| is_free | Integer | 可空 | 0 收费 1 免费 |
| keyword | String | @Size(max=50) | 课程名/讲师名模糊 |
| sort_by | String | 可空 | publish_time / student_count / score |
| page_num / page_size | Integer | @Min(1) | 分页参数 |

---

## 2. 认证与安全设计（JWT + 拦截器 + ThreadLocal）

### 2.1 登录认证流程（含图形验证码）

```
1. GET /api/user/captcha
   → Hutool CircleCaptcha 生成图形验证码
   → StringRedisTemplate 存 captcha:{uuid} = 验证码文本, TTL 300s
   → 返回 CaptchaVO{ uuid, img_base64 }
2. POST /api/user/login { username, password, uuid, captcha }
   → CaptchaService.verifyCaptcha：比对 Redis 文本，通过立即 delete（一次性）
   → UserService 校验账号 + BCrypt.checkpw 密码
   → JwtUtil.generateToken(userId, role)
   → 返回 LoginVO{ token, user_vo }
3. 后续请求 Header: Authorization: Bearer <token>
   → AuthInterceptor.preHandle
      ① 放行 OPTIONS 预检
      ② 剥离 "Bearer " 前缀，validateToken 校验签名/过期
      ③ 查询 Redis Token 黑名单（注销/禁用即时失效）
      ④ 解析 userId/role → UserContext.set(userId, role)
   → Service 内 UserContext.getUserId()/getRole() 取身份（未登录抛 TokenException）
   → afterCompletion: UserContext.remove()  // 防止线程复用串号
```

### 2.2 JwtUtil（对齐 VolunteerSystem 实现）

- `@Component`，`@Value` 注入 `jwt.secret`（Base64 编码）与 `jwt.expiration`（默认 604800000ms = 7 天）。
- 密钥：`Base64.getDecoder().decode(secret)` → `Keys.hmacShaKeyFor()`，懒加载缓存。

| 方法 | 说明 |
| --- | --- |
| `generateToken(String userId, String role)` | subject=userId，自定义 claim `role` |
| `parseToken(token)` / `validateToken(token)` | 解析 Claims / try-parse 返回 boolean |
| `getUserId(token)` / `getRole(token)` | 过期抛 `TokenException("登录已过期")`，其他异常抛 `TokenException("登录失效")` |

### 2.3 拦截器注册（WebMvcConfig implements WebMvcConfigurer）

- `addInterceptors`：拦截 `/api/**`；排除 `/api/user/login`、`/api/user/register`、`/api/user/captcha`、`/api/user/reset-password`、课程/职位公开查询接口（游客可浏览）、`/error`。
- `/api/admin/**`：Service 层 `checkRole()` 二次校验管理员角色，非管理员抛 403。
- `addResourceHandlers`：`/uploads/**` 映射 `file:${file.upload.path}`（头像/简历等本地磁盘目录）。
- `addCorsMappings`：开发期全放行，生产环境收紧为前端域名白名单。

### 2.4 密码与验证码安全

- 注册/重置密码：BCrypt 加盐加密（`BCryptPasswordUtil`）；登录 `BCrypt.checkpw` 比对。
- 图形验证码：Redis `captcha:{uuid}`，TTL 300s，一次性。
- 重置密码邮箱/手机验证码：Redis `edu:reset:{target}`，TTL 5 分钟，一次性。

---

## 3. Redis 缓存设计（双轨制，对齐 VolunteerSystem）

### 3.1 第一轨：Spring Cache 注解缓存（热点读数据）

主类 `@EnableCaching` + `spring.cache.type=redis`；`RedisConfig` 自定义 `RedisCacheManager`（key String 序列化，value GenericJackson2Json 保留类型信息），全局默认 TTL 10 分钟，按 cacheName 细化：

| cacheName | 数据 | TTL | 注解用法 |
| --- | --- | --- | --- |
| `courseList` | 课程分页列表（key 含 type+条件+页码） | 5 min | `@Cacheable` 查询 / 课程变更 `@CacheEvict(allEntries=true)` |
| `courseDetail` | 课程详情 | 10 min | `@Cacheable(key="#id")` / 更新、上下架 `@CacheEvict` |
| `jobDetail` | 职位详情 | 10 min | 同上 |
| `courseCategory`、`jobCategory` | 分类字典 | 24 h | `@Cacheable` + 变更 `@CacheEvict` |

> VolunteerSystem 已搭好该基础设施但未启用注解，本项目要求实际落地 `@Cacheable/@CacheEvict`。

### 3.2 第二轨：StringRedisTemplate 直用（短时效/原子操作）

| 数据 | Key 规范 | TTL | 说明 |
| --- | --- | --- | --- |
| 图形验证码 | `captcha:{uuid}` | 300 s | 验证后立即 delete |
| 重置密码验证码 | `edu:reset:{email/phone}` | 5 min | 验证后立即 delete |
| Token 黑名单 | `edu:token:blacklist:{token}` | 与 Token 剩余有效期一致 | 拦截器命中抛 TokenException(1002) |
| 防重复投递 | `edu:apply:{user_id}:{job_id}` | 职位在架期间 | SETNX，已存在拒绝重复申请 |
| 报名/下单防重锁 | `edu:order:lock:{user_id}:{course_id}` | 10 s | SETNX 分布式锁，防重复下单 |

### 3.3 缓存一致性

- 写路径：管理员编辑/上下架课程或职位 → 更新 DB → `@CacheEvict` 失效对应缓存。
- 击穿：详情查询加 SETNX 互斥锁回源；穿透：不存在的数据缓存空值（TTL 1 min）；雪崩：TTL 加随机抖动（±5 min）。

---

## 4. 数据库设计（MySQL 8）

> 约定：主键 `id BIGINT AUTO_INCREMENT`（`id-type: AUTO`）；逻辑删除 `deleted TINYINT DEFAULT 0`；审计字段 `created_at` / `updated_at`；金额 `DECIMAL(10,2)`；字符集 utf8mb4；`map-underscore-to-camel-case: false`，实体字段直接沿用下划线命名（对齐 VolunteerSystem）。

### 4.1 用户模块

**users 用户表**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT | 主键 |
| username | VARCHAR(32) UNIQUE | 登录账号 |
| password | VARCHAR(100) | BCrypt 密文 |
| nickname | VARCHAR(32) | 昵称 |
| real_name | VARCHAR(32) | 姓名 |
| avatar | VARCHAR(255) | 头像 URL（/uploads/ 前缀） |
| email / phone | VARCHAR(64) | 邮箱 / 手机号 |
| role | TINYINT | 0 学员 1 教师 2 管理员 |
| status | TINYINT | 1 正常 2 封禁 |

```sql
CREATE TABLE `users` (
  `id`         BIGINT       NOT NULL AUTO_INCREMENT,
  `username`   VARCHAR(32)  NOT NULL COMMENT '登录账号',
  `password`   VARCHAR(100) NOT NULL COMMENT 'BCrypt密文',
  `nickname`   VARCHAR(32)  DEFAULT NULL,
  `real_name`  VARCHAR(32)  DEFAULT NULL,
  `avatar`     VARCHAR(255) DEFAULT NULL,
  `email`      VARCHAR(64)  DEFAULT NULL,
  `phone`      VARCHAR(20)  DEFAULT NULL,
  `role`       TINYINT      NOT NULL DEFAULT 0 COMMENT '0学员 1教师 2管理员',
  `status`     TINYINT      NOT NULL DEFAULT 1 COMMENT '1正常 2封禁',
  `deleted`    TINYINT      NOT NULL DEFAULT 0,
  `created_at` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 4.2 课程模块

**course 课程表**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT | 主键 |
| title | VARCHAR(100) | 课程名称（索引，模糊搜索） |
| type | TINYINT | 0 理论 1 实训 2 认证 |
| tech_system_id / tech_direction_id | BIGINT | 技术体系 / 技术方向 |
| level | TINYINT | 0 初级 1 中级 2 高级 3 专业共建合作（实训为实验等级） |
| cover | VARCHAR(255) | 封面图 |
| teacher_id / teacher_name | BIGINT / VARCHAR(32) | 授课教师（姓名冗余便于搜索） |
| courseware_count / video_count / lab_count | INT | 课件 / 视频 / 实验数量（冗余统计） |
| is_free | TINYINT | 0 收费 1 免费 |
| price | DECIMAL(10,2) | 价格（免费为 0） |
| score | DECIMAL(2,1) | 平均评分（评论后重算） |
| rating_count | INT | 评价人次 |
| student_count | INT | 报名学习人数（排序用） |
| intro / target | TEXT | 课程简介 / 课程目标 |
| status | TINYINT | 0 下架 1 上架 |
| publish_time | DATETIME | 上架时间（排序用） |

```sql
CREATE TABLE `course` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(100) NOT NULL,
  `type` TINYINT NOT NULL COMMENT '0理论 1实训 2认证',
  `tech_system_id` BIGINT DEFAULT NULL,
  `tech_direction_id` BIGINT DEFAULT NULL,
  `level` TINYINT DEFAULT 0,
  `cover` VARCHAR(255) DEFAULT NULL,
  `teacher_id` BIGINT DEFAULT NULL,
  `teacher_name` VARCHAR(32) DEFAULT NULL,
  `courseware_count` INT DEFAULT 0,
  `video_count` INT DEFAULT 0,
  `lab_count` INT DEFAULT 0,
  `is_free` TINYINT NOT NULL DEFAULT 1,
  `price` DECIMAL(10,2) DEFAULT 0.00,
  `score` DECIMAL(2,1) DEFAULT 0.0,
  `rating_count` INT DEFAULT 0,
  `student_count` INT DEFAULT 0,
  `intro` TEXT, `target` TEXT,
  `status` TINYINT NOT NULL DEFAULT 1,
  `publish_time` DATETIME DEFAULT NULL,
  `deleted` TINYINT NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_list` (`type`,`status`,`tech_system_id`,`tech_direction_id`),
  KEY `idx_teacher_name` (`teacher_name`),
  KEY `idx_publish_time` (`publish_time`),
  KEY `idx_student_count` (`student_count`),
  KEY `idx_score` (`score`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**其余课程表（字段摘要）：**

| 表 | 关键字段 | 约束/索引 |
| --- | --- | --- |
| course_category 课程分类 | id, parent_id(0=技术体系), name, type, sort | idx(parent_id) |
| course_chapter 课程目录 | id, course_id, title, resource_type(0课件/1视频/2实验), duration, sort | idx(course_id) |
| course_comment 课程评论 | id, course_id, user_id, score(1~5), content, status(0隐藏/1正常) | uk(course_id,user_id) |
| course_qa 课程答疑 | id, course_id, user_id, question, answer, answerer_id, status(0待回复/1已回复) | idx(course_id) |
| course_collection 课程收藏 | id, user_id, course_id | uk(user_id,course_id) |
| course_enrollment 报名记录 | id, user_id, course_id, order_id(免费为NULL), progress, finish_count, total_count | uk(user_id,course_id) |
| study_record 学习明细 | id, user_id, course_id, chapter_id | uk(user_id,chapter_id) |

### 4.3 实习就业模块

| 表 | 关键字段 | 约束/索引 |
| --- | --- | --- |
| company 公司 | id, name, logo, industry, scale, region, intro | idx(name) |
| job_category 职位分类 | id, parent_id(0=一级分类), name, sort | idx(parent_id) |
| job 职位 | id, title, city, address, salary_min, salary_max, headcount, company_id, category_id, description, requirement, status | idx(status,category_id)、idx(title) |
| job_collection 职位收藏 | id, user_id, job_id | uk(user_id,job_id) |
| job_application 职位申请 | id, user_id, job_id, resume_url, status(0待处理/1已查看/2通过/3拒绝) | uk(user_id,job_id) |
| ai_interview AI 面试 | id, user_id, job_id, application_id, status(0待进行/1进行中/2已完成), report, interview_time | idx(user_id,job_id) |

### 4.4 订单模块

| 表 | 关键字段 | 约束/索引 |
| --- | --- | --- |
| order_info 订单 | id, order_no VARCHAR(32), user_id, course_id, amount, pay_type, status(0待支付/1已支付/2已退款/3已取消), pay_time | uk(order_no)、idx(user_id)、idx(status,created_at) |

### 4.5 MyBatis-Plus 使用约定

- `mapper-locations: classpath:mapper/*.xml`；`log-impl: StdOutImpl`（开发期 SQL 日志）。
- Service 继承 `ServiceImpl<XxxMapper, Entity>`，单表操作统一 `lambdaQuery()/lambdaUpdate()` 链式。
- 复杂多表 JOIN（如课程详情含教师、职位详情含公司）写 Mapper XML，`resultType` 直接映射 VO。
- **本项目新增**（VolunteerSystem 未配置，作为增强）：`MybatisPlusInterceptor + PaginationInnerInterceptor` 分页插件；`MetaObjectHandler` 自动填充 `created_at` / `updated_at`。

---

## 5. 核心接口设计（RESTful，统一前缀 /api）

响应结构 `Result<T> = { code, msg, data }`；分页返回 `{ records, total, current, size }`（MyBatis-Plus Page）。

### 5.0 统一响应与业务错误码（对齐 VolunteerSystem 异常体系）

**Result 静态工厂**：`success(msg, data)` / `success(data)` / `success(msg)`（code=200）、`error(code, msg)`。

**异常体系**：`BaseBusinessException extends RuntimeException`（持有 `int code`），各业务子类固定错误码分段：

| 异常类 | code | 触发场景 |
| --- | --- | --- |
| RegisterException | 1000 | 用户名已存在、注册参数非法 |
| LoginException | 1001 | 账号或密码错误、用户被封禁 |
| TokenException | 1002 | Token 缺失/过期/失效/黑名单命中（前端收到 1002 清除登录态） |
| CaptchaException | 1003 | 图形/邮箱验证码错误、过期 |
| ProfileException | 1004 | 资料修改失败 |
| CourseException | 1010 | 课程不存在/已下架、重复报名/收藏/评论 |
| JobException | 1011 | 职位不存在/已下架、重复投递、未投递先申请面试 |
| OrderException | 1012 | 订单状态非法、退款失败 |
| AdminException | 1013 | 非管理员访问后台、管理操作非法 |

**GlobalExceptionHandler**（`@RestControllerAdvice + @Slf4j`）：`BaseBusinessException` 透传 code/msg；`MethodArgumentNotValidException` / `ConstraintViolationException` 取首个字段错误返回 400；兜底 `Exception` 返回 500"系统繁忙，请稍后重试"。

### 5.1 用户模块（UserController）

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| GET | `/user/captcha` | 图形验证码 → CaptchaVO{uuid, img_base64} | 公开（白名单） |
| POST | `/user/register` | 注册（RegisterDTO） | 公开 |
| POST | `/user/login` | 验证码 + 登录（LoginDTO）→ LoginVO{token, user_vo} | 公开 |
| POST | `/user/reset-password` | 邮箱/手机验证码重置密码 | 公开 |
| GET | `/user/profile` | 当前用户信息 | 登录 |
| PUT | `/user/profile` | 修改昵称/姓名（UpdateProfileDTO） | 登录 |
| PUT | `/user/avatar` | 头像上传（multipart ≤ 2MB，存 `file.upload.path`） | 登录 |
| POST | `/user/logout` | 注销（Token 加黑名单） | 登录 |

### 5.2 课程模块（CourseController）

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| GET | `/course/categories` | 技术体系/技术方向两级字典（缓存 24h） | 公开 |
| GET | `/course/page` | 课程分页（CourseQueryDTO，@Cacheable courseList） | 公开 |
| GET | `/course/{id}` | 课程详情 CourseDetailVO（教师简介、收藏状态、学习进展、是否已报名） | 公开/登录增强 |
| GET | `/course/{id}/chapters` | 课程目录 | 公开 |
| POST | `/course/{id}/collect` | 收藏/取消收藏（切换，幂等） | 登录 |
| POST | `/course/{id}/enroll` | 立即学习：免费直接报名；收费生成待支付订单 | 登录 |
| GET | `/course/{id}/comments` | 评论分页 | 公开 |
| POST | `/course/{id}/comments` | 发表评论+评分（须已报名，每人一条） | 登录 |
| GET | `/course/{id}/questions` | 答疑列表 | 公开 |
| POST | `/course/{id}/questions` | 提交答疑问题 | 登录 |
| POST | `/study/chapters/{chapterId}/finish` | 标记资源学完，更新 progress | 登录 |

### 5.3 实习就业模块（JobController）

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| GET | `/job/categories` | 职位分类树（两级，前端切片翻页，缓存 24h） | 公开 |
| GET | `/job/page` | 职位分页：category_id、search_type(job/company)、keyword、page_num | 公开 |
| GET | `/job/{id}` | 职位详情 JobDetailVO（XML JOIN 公司表直接映射 VO） | 公开/登录增强 |
| POST | `/job/{id}/collect` | 感兴趣（收藏切换，幂等） | 登录 |
| POST | `/job/{id}/apply` | 申请职位（SETNX + uk 双重防重） | 登录 |
| POST | `/job/{id}/ai-interview` | 申请数字人面试（须已投递） | 登录 |

### 5.4 后台管理模块（AdminController，Service 层 checkRole() 二次校验）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/PUT | `/admin/users`、`/admin/users/{id}/status` | 用户分页、启用/封禁（封禁即拉黑其 Token） |
| GET/PUT | `/admin/orders`、`/admin/orders/{id}/refund` | 订单分页、退款（退款作废报名记录） |
| POST/PUT | `/admin/courses`、`/admin/courses/{id}/status` | 课程新增/编辑、上/下架（@CacheEvict 同步失效缓存） |
| POST/PUT | `/admin/jobs`、`/admin/jobs/{id}/status` | 职位新增/编辑、上/下架（同上） |
| GET/PUT | `/admin/applications`、`/admin/applications/{id}/status` | 职位申请状态流转 |
| GET/PUT | `/admin/interviews`、`/admin/interviews/{id}` | AI 面试申请处理 |
| DELETE/PUT | `/admin/comments/{id}`、`/admin/questions/{id}/answer` | 违规评论隐藏、答疑回复 |

---

## 6. 关键业务流程

### 6.1 课程报名（收费）

```
点击"立即学习" → UserContext.getUserId() 取身份 → 查询课程
 → 免费? 直接写入 course_enrollment(uk 防重)
 : SETNX edu:order:lock(10s) → 创建 order_info(待支付) → 支付回调
 → 订单=已支付 → 写 course_enrollment → student_count+1
 → @CacheEvict(courseList/courseDetail) → 释放锁
```

### 6.2 学习进展更新

```
POST /study/chapters/{id}/finish → 写 study_record(uk 防重)
 → 统计 finish_count / total_count → 更新 course_enrollment.progress
 → @CacheEvict(courseDetail, key=#courseId)
```

### 6.3 AI 面试申请

```
点击"数字人面试" → 校验 job_application 已存在(未投递抛 JobException 1011)
 → 创建 ai_interview(待进行) → 返回面试会话标识
 → 前端跳转数字人面试页 → 完成后回写 status 与 report
```

### 6.4 后台上下架与缓存联动

```
管理员上下架课程/职位 → lambdaUpdate 更新 status
 → @CacheEvict 失效列表/详情缓存 → 前台下一请求回源 DB 重建
```

### 6.5 定时任务（task 包，对齐 VolunteerSystem 的 ActivityStatusTask 模式）

- 每日 0 点（cron `0 0 0 * * ?`）扫描刷新：过期职位自动下架、超时未支付订单自动取消。

---

## 7. 前端设计（Vue 3 + TS，对齐 VolunteerSystem 前端组织）

### 7.1 目录结构（无 Pinia、无 UI 组件库）

```
src/
├── api/         request.ts(axios封装) + user/course/job/order/admin.ts
├── assets/      base.css、main.css
├── components/  AppToast、AuthLayout、CourseCard、JobCard、CategoryFilter、Pagination
├── composables/ toast.ts
├── router/      index.ts（三级路由守卫）
├── stores/      auth.ts（reactive 模块单例 + localStorage, key=edu_platform_token）
├── types/       api.ts（与后端逐字段对齐的 DTO/VO 类型 + ROLE/STATUS 常量）
└── views/       Login/Register/Reset/Profile
                 CourseList(理论/实训/认证复用)/CourseDetail(四Tab)
                 JobList(分类导航+搜索)/JobDetail(感兴趣/申请/数字人面试)
                 Admin(用户/订单/课程/职位/申请管理)
```

### 7.2 关键约定

- `request.ts`：`baseURL:'/api'`；请求拦截器自动带 `Bearer {token}`；响应统一拆 `Result`，code≠200 抛错，**code===1002 时 clearAuth() 并跳转登录**。
- `stores/auth.ts`：`reactive` 单例 + localStorage 持久化（token、userInfo）。
- 路由守卫：`meta.requiresAuth` / `meta.guest` / `meta.adminOnly` 三级，登录后懒拉取 profile 判断角色。
- `vite.config.ts`：dev 代理 `/api` 与 `/uploads` → `http://localhost:8080`。
- `types/api.ts` 与后端 VO/DTO 字段逐一对齐（下划线命名），避免前后端字段漂移。

---

## 8. 部署方案

- 后端：Jar 包部署（或 Docker），Nginx 反向代理 `/api` 至后端、`/` 指向前端 dist、`/uploads` 指向文件目录。
- 前端：`npm run build` 产物静态托管 Nginx。
- MySQL/Redis：独立实例，Redis 开启 AOF 持久化。
- 文件存储：本地磁盘 `file.upload.path`（头像/简历），multipart 限制单文件 5MB。
- 环境分离：dev / prod 两套 `application-{env}.yml`。

### application.yml 关键配置

```yaml
spring:
  datasource: { url: jdbc:mysql://localhost:3306/edu_platform, username: root, password: ****** }
  data.redis: { host: localhost, port: 6379, database: 0 }
  cache.type: redis
  servlet.multipart: { max-file-size: 5MB, max-request-size: 5MB }
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration: { log-impl: org.apache.ibatis.logging.stdout.StdOutImpl, map-underscore-to-camel-case: false }
  global-config.db-config.id-type: AUTO
jwt:
  secret: <Base64编码密钥>
  expiration: 604800000        # 7天
file.upload.path: D:\Work\upload
```

---

## 9. 非功能性设计要点

| 项 | 设计 |
| --- | --- |
| 全局异常 | GlobalExceptionHandler 分类型处理，业务错误码 1000~1013 分段，Token 失效固定 1002 |
| 参数校验 | DTO 使用 `jakarta.validation` 注解 + `@Valid`；避免裸 String 收参等非常规绑定 |
| 幂等设计 | 收藏/报名/投递：DB 唯一约束兜底 + Redis SETNX 前置拦截 |
| 日志 | SLF4J + Logback，关键操作（报名、投递、上下架、退款）记录业务日志 |
| 对象转换 | 统一 `Converter.INSTANCE`（MapStruct），禁止 Controller 手工 set 字段 |
| 测试 | 单测 JUnit5 + Mockito；接口测试 Postman；重点覆盖验证码一次性、防重幂等、鉴权越权、缓存失效场景 |
| 生产安全 | CORS 收紧域名白名单；jwt.secret 改为环境变量注入，禁止明文提交 |
